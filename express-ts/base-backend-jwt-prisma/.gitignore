# Dependencies
node_modules/

# Prisma Client, written by `prisma generate` (which `npm install` runs).
# Generated code in git is a merge conflict waiting to happen, and it is stale
# the moment somebody edits prisma/schema.prisma without regenerating.
src/generated/

# Build output
dist/
out/
build/
*.tsbuildinfo

# Tests & coverage
coverage/
.nyc_output/

# Logs
logs/
*.log
npm-debug.log*

# Environment. Committed: .env.development (throwaway dev values),
# .env.test (the suite's contract, nothing secret) and the production
# template. Real production secrets live in .env.production, which is not.
.env
.env.*
!.env.development
!.env.test
!.env.production.example

# Caches
.eslintcache
.cache/

# Editors & OS
.idea/
*.swp
.DS_Store
Thumbs.db

# One JSON object per line so a log shipper can index fields instead of grepping
# a rendered message. MDC entries, and so requestId, are included by the format.
logging.structured.format.console=ecs
logging.level.root=INFO

springdoc.api-docs.enabled=false
springdoc.swagger-ui.enabled=false

# Copy to .env and fill in. .env is git ignored and must stay that way.
# For the password, use something generated, for example: openssl rand -base64 24
POSTGRES_DB=simplebackend
POSTGRES_USER=simplebackend
POSTGRES_PASSWORD=
